# GL-3DPRT-SP-M 运动范围判定说明

版本：v1.7

日期：2026-06-17

## 这个工具是做什么的

这个工具用来判断一个长方体能不能放进 GL-3DPRT-SP-M 的运动范围里。

简单说：

- 绿色透明区域表示设备能到达的空间。
- 黄色长方体表示你输入的尺寸。
- 如果长方体完全在绿色范围内，就显示“长方体在范围内。”
- 如果有一部分超出去，就会提示不可达。

## 怎么看这个 3D 图

- 透明绿色体：设备可达范围
- 黄色长方体：当前输入的长方体
- 红色长方体：当前尺寸或位置不可达
- 淡黄色设备模型：设备外形参考，只是帮助看方向和位置，不参与判定
- X 方向：左右方向
- Y 方向：前后方向
- Z 方向：高度方向
- Z=0：地面

鼠标操作：

- 拖动鼠标：旋转视角
- 滚轮：放大或缩小
- 右上角“视图”：快速切换默认、前、上、右视图

当前版本已内嵌压缩后的设备模型。网页打开时不会自动加载或解码模型，只显示运动范围、长方体和 UI。

需要查看设备外形时，点击左侧的“显示设备模型”。模型加载完成后，按钮会变成“隐藏设备模型”。这个设备模型只是参考外形，不参与可达范围判定，也不会影响最大贴合结果。

设备模型显示姿态：`rotation.x = -Math.PI / 2`，`rotation.y = -Math.PI / 2`，`rotation.z = Math.PI`。模型位置按包围盒居中，Y 方向基准为 `2500`，底部对齐到 `Z=0`。

## 左侧参数怎么填

### X 向位置

X 方向固定在 `0`，也就是长方体左右居中放置。

这个值不能修改。

### Y 向内侧边位置

这个值表示长方体靠近设备内侧的那一条边在 Y 方向的位置。

- 如果 Y 是正数，长方体从这个位置向 Y+ 方向展开。
- 如果 Y 是负数，长方体从这个位置向 Y- 方向展开。

### 底面高度 Z

底面高度固定为 `0`，也就是长方体放在地面上。

长方体只向 Z+ 方向，也就是向上展开。

### L / W / H

- L：长方体在 X 方向的长度
- W：长方体在 Y 方向的宽度
- H：长方体在 Z 方向的高度

当前最大输入值：

- L 最大值：`20000 mm`
- W 最大值：`12000 mm`
- H 最大值：`10000 mm`

默认尺寸为：

```text
L x W x H = 2000 x 2000 x 2000 mm
```

## 最大贴合按钮

左侧有三个最大贴合按钮：

- 固定L最大贴合
- 固定W最大贴合
- 固定H最大贴合

它们的意思是：

- 固定L最大贴合：保持当前 L 不变，先找最大的 W，再找最大的 H。
- 固定W最大贴合：保持当前 W 不变，先找最大的 L，再找最大的 H。
- 固定H最大贴合：保持当前 H 不变，搜索更合适的 L/W 底面积组合。

最大贴合只是在当前设备运动范围内寻找一个合适长方体，不代表唯一解。

## 当前设备范围参数

### XY 平面

水平可达范围为：

- 外圆：圆心 `(0, 0)`，半径 `10187 mm`
- 内圆：圆心 `(0, -1118)`，半径 `3115.5 mm`

内圆区域按不可达区域处理，相当于一个沿 Z 方向贯穿的不可达圆柱。

### R-Z 垂直截面

垂直截面使用以下实测点生成，并做平滑处理：

```text
(R,Z)
(1600,0)
(10187,0)
(10129,1467)
(9904,3075)
(9416,4651)
(8700,6009)
(7598,7492)
(6809,8442)
(6192,9175)
(5861,9472)
(5549,9602)
(5400,9565)
(5149,9175)
(4970,8325)
(4850,7561)
(4642,6802)
(3677,6097)
(2716,5911)
(1600,5872)
```

R-Z 截面外的区域全部按不可达处理。

## 判定方法

一个点必须同时满足两件事，才算可达：

- 在 XY 平面的可达区域内
- 在 R-Z 垂直截面的可达区域内

一个长方体必须整体都在这个交集空间内，才会被判定为“在范围内”。

工具会检查长方体角点和表面采样点，避免只看角点导致误判。

## 文件说明

当前网页主文件是：

```text
outputs/GL-3DPRT-SP-M.html
```

设备选择页是：

```text
outputs/index.html
```

说明文档是：

```text
outputs/GL-3DPRT-SP-M说明.md
```

分享给别人时，可以直接发送 SP-M 的 HTML 文件；如果要同时提供 SP-M 和 SP-S 两个入口，分享 `outputs/index.html`。

## 版本和日期怎么维护

每次把项目推送到 GitHub 前，都要同步检查并更新：

- 网页标题里的版本号
- 左侧信息栏里的版本号和日期
- 本说明文档顶部的版本号和日期

只要网页功能、数据、模型、显示效果或说明文档发生变化，就递增版本号。日期使用当天日期。
